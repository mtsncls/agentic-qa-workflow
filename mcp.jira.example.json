{
  "_comment": "Example MCP servers for using Jira/Confluence from the Claude agents. Copy to .mcp.json (repo root) or pass mcpServers in the SDK query() options.",
  "mcpServers": {
    "atlassian": {
      "url": "https://mcp.atlassian.com/v1/sse"
    },
    "jira-local": {
      "command": "npx",
      "args": ["-y", "jira-mcp"],
      "env": {
        "JIRA_URL": "https://your-company.atlassian.net",
        "JIRA_API_TOKEN": "${JIRA_API_TOKEN}"
      }
    }
  }
}
