{
  "_comment": "Ejemplo de servidores MCP para usar Jira/Confluence desde los agentes Claude. Copia a .mcp.json (raíz del repo) o pasa mcpServers en las opciones de query() del SDK.",
  "mcpServers": {
    "atlassian": {
      "url": "https://mcp.atlassian.com/v1/sse"
    },
    "jira-local": {
      "command": "npx",
      "args": ["-y", "jira-mcp"],
      "env": {
        "JIRA_URL": "https://tu-empresa.atlassian.net",
        "JIRA_API_TOKEN": "${JIRA_API_TOKEN}"
      }
    }
  }
}
